# Python only treats folders as "packages" if they have an __init__.py

# When you run a script directly, Python doesn't automatically include sibling folders in the import path — unless it's run from the right location or with PYTHONPATH

# thats why this file exists.